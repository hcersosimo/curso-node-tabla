# NOTAS

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base de la tabla de multiplicar             [number] [required]
  -l, --listar   lista la tabla de multiplicar por consola
                                                      [boolean] [default: false]
  -h, --hasta    numero hasta de la tabla de multiplicar por consola
                                                          [number] [default: 10]
```                                                          